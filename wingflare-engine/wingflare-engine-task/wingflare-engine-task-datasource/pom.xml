<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>com.wingflare</groupId>
        <artifactId>wingflare-engine-task</artifactId>
        <version>${revision}</version>
        <relativePath>../pom.xml</relativePath>
    </parent>
    <artifactId>wingflare-engine-task-datasource</artifactId>
    <packaging>pom</packaging>

    <properties>
        <mybatis-spring.version>3.0.3</mybatis-spring.version>
        <mybatis-plus.version>3.5.9</mybatis-plus.version>
        <dameng.version>8.1.3.62</dameng.version>
        <mariadb.version>3.1.4</mariadb.version>
        <kingbase.version>8.6.0</kingbase.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.mybatis</groupId>
                <artifactId>mybatis-spring</artifactId>
                <version>${mybatis-spring.version}</version>
            </dependency>

            <dependency>
                <groupId>com.baomidou</groupId>
                <artifactId>mybatis-plus-spring-boot3-starter</artifactId>
                <version>${mybatis-plus.version}</version>
            </dependency>

            <!-- >= 3.5.9 PaginationInnerInterceptor 需要单独引入 -->
            <dependency>
                <groupId>com.baomidou</groupId>
                <artifactId>mybatis-plus-jsqlparser</artifactId>
                <version>${mybatis-plus.version}</version>
            </dependency>

            <dependency>
                <groupId>com.dameng</groupId>
                <artifactId>DmJdbcDriver18</artifactId>
                <version>${dameng.version}</version>
            </dependency>

            <dependency>
                <groupId>cn.com.kingbase</groupId>
                <artifactId>kingbase8</artifactId>
                <version>${kingbase.version}</version>
            </dependency>

            <dependency>
                <groupId>org.mariadb.jdbc</groupId>
                <artifactId>mariadb-java-client</artifactId>
                <version>${mariadb.version}</version>
            </dependency>

            <dependency>
                <groupId>com.wingflare</groupId>
                <artifactId>wingflare-engine-task-datasource-template</artifactId>
                <version>${project.parent.version}</version>
            </dependency>

        </dependencies>

    </dependencyManagement>

    <modules>
        <module>wingflare-engine-task-datasource-template</module>
        <module>wingflare-engine-task-datasource-dm8</module>
        <module>wingflare-engine-task-datasource-kingbase</module>
        <module>wingflare-engine-task-datasource-mariadb</module>
        <module>wingflare-engine-task-datasource-mysql</module>
        <module>wingflare-engine-task-datasource-oracle</module>
        <module>wingflare-engine-task-datasource-postgres</module>
        <module>wingflare-engine-task-datasource-sqlserver</module>
    </modules>

</project>