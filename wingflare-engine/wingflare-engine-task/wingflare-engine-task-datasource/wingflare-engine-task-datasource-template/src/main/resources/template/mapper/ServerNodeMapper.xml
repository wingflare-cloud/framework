<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wingflare.task.datasource.template.persistence.mapper.ServerNodeMapper">

    <select id="selectActivePodCount"
            resultType="com.wingflare.task.datasource.template.persistence.dataobject.ActivePodQuantityResponseDO">
        SELECT node_type AS nodeType,
               COUNT(*)  AS total
          FROM wf_task_server_node
        ${ew.customSqlSegment}
    </select>

</mapper>